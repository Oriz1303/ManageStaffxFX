package com.example.managestaff.model.services;


public class Paginations {

//    TableView<MyData> tableView = new TableView<>();
//    Pagination pagination = new Pagination();
//
//
//    TableColumn<MyData, String> nameColumn = new TableColumn<>("Name");
//nameColumn.setCellValueFactory(new PropertyValueFactory<>("name"));
//
//// Add more columns...
//
//tableView.getColumns().addAll(nameColumn, /* other columns... */);
//
//    ObservableList<MyData> dataList = FXCollections.observableArrayList();
//// Add your data to the list...
//
//
//    final int itemsPerPage = 10; // Number of items per page
//    final int totalPages = (dataList.size() / itemsPerPage) + 1; // Calculate total pages
//
//pagination.setPageCount(totalPages);
//
//pagination.setPageFactory(pageIndex -> {
//        int fromIndex = pageIndex * itemsPerPage;
//        int toIndex = Math.min(fromIndex + itemsPerPage, dataList.size());
//        tableView.setItems(FXCollections.observableArrayList(dataList.subList(fromIndex, toIndex)));
//        return tableView;
//    });
//
//
//
//
//    VBox root = new VBox(tableView, pagination);
//    Scene scene = new Scene(root);
//primaryStage.setScene(scene);
//primaryStage.show();


}
